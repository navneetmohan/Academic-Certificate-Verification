{
  "name": "blockchain-academic-certificate-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Blockchain-Based Academic Certificate Verification System",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.2",
    "hardhat": "^2.22.5"
  }
}
